# Traveloop Backend API

Backend API for Traveloop, a personalized travel planning platform.

## Setup

1. Copy `.env.example` to `.env` and fill in the values
2. Run `npm install`
3. Run `npm run dev` to start the development server
